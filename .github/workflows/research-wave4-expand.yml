name: Publish technology Plug-in and embedding research

on:
  push:
    branches:
      - docs/technology-plugins-embedding
    paths:
      - transfer/research-wave4/**
      - .github/workflows/research-wave4-expand.yml
  pull_request:
    branches:
      - main
    paths:
      - transfer/research-wave4/**
      - .github/workflows/research-wave4-expand.yml

permissions:
  contents: write

concurrency:
  group: research-wave4-expand
  cancel-in-progress: false

jobs:
  expand:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out publication branch
        uses: actions/checkout@v4
        with:
          ref: docs/technology-plugins-embedding
          fetch-depth: 0
          show-progress: false

      - name: Reconstruct and verify documentation generator
        shell: bash
        run: |
          : > tools/research_wave4_generator.py
          for part in transfer/research-wave4/source-*; do
            cat "$part" >> tools/research_wave4_generator.py
            printf '\n' >> tools/research_wave4_generator.py
          done
          actual="$(sha256sum tools/research_wave4_generator.py | cut -d' ' -f1)"
          printf '%s\n' "$actual" > transfer/research-wave4/actual-sha.txt
          set +e
          python3 -m py_compile tools/research_wave4_generator.py > transfer/research-wave4/compile.log 2>&1
          compile_status=$?
          set -e
          if [ "$compile_status" -ne 0 ] || [ "$actual" != "3e9c10f87dbda366d0d54825bc9e0e3aa2b7835b9840e7f29e50eb16edcd2b4d" ]; then
            printf 'compile_status=%s\n' "$compile_status" > transfer/research-wave4/diagnostic-status.txt
            git config user.name "turing-research[bot]"
            git config user.email "turing-research[bot]@users.noreply.github.com"
            git add transfer/research-wave4/actual-sha.txt transfer/research-wave4/compile.log transfer/research-wave4/diagnostic-status.txt
            git commit -m "Record research wave 4 generator diagnostics"
            git push origin HEAD:docs/technology-plugins-embedding
            exit 1
          fi

      - name: Generate technology Plug-in and embedding documentation
        run: python3 tools/research_wave4_generator.py

      - name: Remove one-shot publication machinery
        shell: bash
        run: |
          rm -f tools/research_wave4_generator.py
          rm -rf transfer/research-wave4
          rmdir transfer 2>/dev/null || true
          rm -f .github/workflows/research-wave4-expand.yml

      - name: Validate repository and prototype
        run: |
          python3 tools/validate_blueprint.py
          cargo fmt --manifest-path prototype/Cargo.toml -- --check
          cargo test --manifest-path prototype/Cargo.toml --all-targets
          cargo run --manifest-path prototype/Cargo.toml --quiet

      - name: Commit clean generated documentation
        shell: bash
        run: |
          git config user.name "turing-research[bot]"
          git config user.email "turing-research[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 1
          git commit -m "Document technology stack Plug-ins and embedding SDKs"
          git push origin HEAD:docs/technology-plugins-embedding
