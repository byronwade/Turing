name: Repository validation

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: repository-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      PYTHONDONTWRITEBYTECODE: '1'
      CARGO_TARGET_DIR: /tmp/turing-repository-validation-target

    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          show-progress: false

      - name: Show pinned environment
        run: |
          rustc --version
          cargo --version
          rustfmt --version
          cargo clippy --version
          python3 --version
          git --version

      - name: Run read-only doctor
        run: cargo run --locked -p xtask -- doctor --ci

      - name: Validate documentation and program records
        shell: bash
        run: |
          set +e
          python3 -B tools/validate_blueprint.py > repository-validation.log 2>&1
          status=$?
          cat repository-validation.log
          exit "$status"

      - name: Validate GitHub issue handoff records
        shell: bash
        run: |
          set +e
          python3 -B tools/validate_github_issue_handoff.py > github-issue-handoff-validation.log 2>&1
          status=$?
          cat github-issue-handoff-validation.log
          exit "$status"

      - name: Validate ADR-0009 evidence records
        shell: bash
        run: |
          set +e
          python3 -B tools/validate_adr_0009_evidence.py > adr-0009-evidence-validation.log 2>&1
          status=$?
          cat adr-0009-evidence-validation.log
          exit "$status"

      - name: Check committed diff whitespace
        shell: bash
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
          PUSH_HEAD_SHA: ${{ github.sha }}
        run: |
          set +e
          if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
            base="$PR_BASE_SHA"
            head="$PR_HEAD_SHA"
          else
            base="$PUSH_BEFORE_SHA"
            head="$PUSH_HEAD_SHA"
          fi
          if [ -z "$base" ] || [[ "$base" =~ ^0+$ ]]; then
            git diff-tree --check --root -r "$head" > diff-whitespace.log 2>&1
          else
            git diff --check "$base" "$head" > diff-whitespace.log 2>&1
          fi
          status=$?
          cat diff-whitespace.log
          exit "$status"

      - name: Validate executable build foundation
        shell: bash
        run: |
          set +e
          python3 -B tools/validate_build_foundation.py > build-foundation-validation.log 2>&1
          status=$?
          cat build-foundation-validation.log
          exit "$status"

      - name: Upload validation diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: repository-validation-${{ github.run_id }}
          path: |
            repository-validation.log
            github-issue-handoff-validation.log
            adr-0009-evidence-validation.log
            diff-whitespace.log
            build-foundation-validation.log
          if-no-files-found: error
          retention-days: 7

      - name: Enforce same-change documentation
        if: github.event_name == 'pull_request'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: python3 -B tools/check_documentation_change.py "$BASE_SHA" "$HEAD_SHA"

      - name: Format workspace
        shell: bash
        run: |
          set +e
          cargo fmt --all -- --check > rustfmt.log 2>&1
          status=$?
          cat rustfmt.log
          exit "$status"

      - name: Upload formatting diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rustfmt-${{ github.run_id }}
          path: rustfmt.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Lint workspace
        shell: bash
        run: |
          set +e
          cargo clippy --workspace --all-targets --all-features --locked -- -D warnings > clippy.log 2>&1
          status=$?
          cat clippy.log
          exit "$status"

      - name: Upload Clippy diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: clippy-${{ github.run_id }}
          path: clippy.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Test workspace
        run: cargo test --workspace --all-targets --locked

      - name: Run shell self-test
        run: cargo run --locked -p turing-shell -- --self-test

      - name: Run architecture prototype
        run: cargo run --locked -p turing-architecture-prototype --quiet

      - name: Enforce clean source tree
        run: |
          test ! -e target
          test ! -e prototype/target
          test ! -e tools/__pycache__
          test ! -e transfer
